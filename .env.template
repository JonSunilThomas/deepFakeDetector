# ══════════════════════════════════════════════════════════════
#  DeepFake Detector — Environment Variables
# ══════════════════════════════════════════════════════════════
# Copy this file:  cp .env.template .env
# Fill in the values below, then start the app.

# ── Frontend (Vercel) ─────────────────────────────────────────
# Set this to your Render backend URL after deploying
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Backend (Render) ─────────────────────────────────────────
# Base URL where ML weights (.pth files) are hosted
# Examples:
#   https://github.com/<user>/DeepFackeDetector/releases/download/v1.0
#   https://huggingface.co/<user>/deepfake-weights/resolve/main
WEIGHTS_BASE_URL=

# ── Optional: Full Backend Mode ──────────────────────────────
# Not needed for server_lite.py, but required for main.py
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/deepfake
# REDIS_URL=redis://localhost:6379/0
# JWT_SECRET=change-me-to-a-random-string
# PRIVATE_KEY=0x...your-deployer-private-key
# CONTRACT_ADDRESS=0x...deployed-contract-address
