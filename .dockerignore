# ── Docker build ignore ───────────────────────────────────────
# Keep the Docker context lean — only backend + ml weights needed.

.git
.github
.venv
venv
__pycache__

# Frontend (deployed separately on Vercel)
frontend/node_modules
frontend/.next
frontend/out

# Docs & config not needed at runtime
docs/
*.md
LICENSE
Makefile
render.yaml
docker-compose.yml
.env
.env.*
!.env.template

# Data & notebooks
ml/datasets/
*.ipynb
notebooks/

# IDE
.vscode/
.idea/
